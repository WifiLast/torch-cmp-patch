# Generated by tools/build_trt_engine.py / tools/trt_onnx_conv3d.py --
# NEVER commit these. TensorRT engines (and their tactic timing caches)
# are compiled/measured against the exact GPU + TensorRT version that
# built them; they are not portable and are meaningless on any other
# machine (including CI). Rebuild locally instead.
*.trt
*.engine
*.cache
